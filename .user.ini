; PHP upload limits for CMSR admin media (videos/images).
; cPanel reads this from the site root when the app is deployed outside public/.
upload_max_filesize = 512M
post_max_size = 520M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
